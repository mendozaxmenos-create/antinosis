DATABASE_URL="file:./dev.db"

# URL pública de la app (misma que uses en el navegador). Ej. http://localhost:3000
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google Calendar — crear credenciales OAuth en Google Cloud Console (tipo Web)
# Redirect URI autorizado: {NEXT_PUBLIC_APP_URL}/api/google-calendar/callback
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
