/** Tipos de gasto que cuentan como “total a pagar” del resumen (coherente con `statementPaymentService`). */
export const STATEMENT_IMPORT_EXPENSE_SOURCE_TYPES = ["imported_file", "imported_pdf", "imported_manual"] as const;
